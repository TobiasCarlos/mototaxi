import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tcb.taxi',
  appName: 'C:/Program Files/Git/',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
